{
  "name": "epk",
  "version": "1.0.0",
  "description": "A simple Elm site styled with Tailwind CSS",
  "scripts": {
    "dev": "npm run build && python3 -m http.server 9999 --directory ./dist",
    "build": "sh ./build.sh"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
